1. Before launching Spring Boot server, must add postgreSQL dependecy to pom.xml (it's probably already there) and
specify pgSQL configurations in the application.properties file

2. 'ctr + F9' to rebuild the project between browser refreshes (enable live-load plugin on the browser
webpage to autoload changes)

3. Set annotations on classes that Spring will use

4. Use double curly braces {{}} for to tell Spring/thymeleaf to format html 'variables'

